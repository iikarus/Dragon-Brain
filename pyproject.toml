[project]
name = "dragon-brain"
version = "1.0.1"
description = "Open-source long-term memory MCP server with knowledge graph and vector search"
requires-python = ">=3.12"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "iikarus"}]
keywords = ["dragon-brain", "mcp", "mcp-server", "ai-memory", "persistent-memory", "knowledge-graph", "vector-search", "falkordb", "qdrant", "ai-agent", "claude", "llm"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "graphiti-core>=0.5.0,<1.0.0",
    "falkordb>=1.0.0",
    "mcp>=0.1.0",
    "pydantic>=2.0.0",
    "sentence-transformers>=2.2.0",
    "scikit-learn>=1.3.0",
    "numpy>=1.24.0",
    "streamlit>=1.30.0",
    "qdrant-client>=1.7.0",
    "pyvis>=0.3.2",
    "redis>=5.0.0",
    "fastapi>=0.100.0",
    "uvicorn>=0.20.0",
    "httpx>=0.26.0",
    "nest-asyncio>=1.6.0",
    "anyio>=4.0.0",
]

[project.urls]
Homepage = "https://github.com/iikarus/Dragon-Brain"
Repository = "https://github.com/iikarus/Dragon-Brain"
Documentation = "https://github.com/iikarus/Dragon-Brain/tree/master/docs"
Issues = "https://github.com/iikarus/Dragon-Brain/issues"

[project.scripts]
dragon-brain = "claude_memory.server:main"
claude-memory = "claude_memory.server:main"

[project.optional-dependencies]
dev = [
    # 1. Automation
    "pre-commit>=3.6.0",
    # 2. Linting/Formatting
    "ruff>=0.1.0",
    # 3. Testing
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    # 4. Coverage
    "pytest-cov>=4.1.0",
    # 5. Coverage Engine
    "coverage>=7.4.0",
    # 6. Property Testing
    "hypothesis>=6.90.0",
    # 7. Flakiness Hunter
    "pytest-randomly>=3.15.0",
    # 8. Quality Gate
    "diff-cover>=9.0.0",
    # 9. Symbolic Execution
    "crosshair-tool>=0.0.74",
    # 10. Static Analysis
    "semgrep>=1.50.0",
    # 11. Security Linter
    "bandit>=1.7.0",
    # 12. Dependency Security
    "pip-audit>=2.7.0",
    # 13. Secret Leakage
    "detect-secrets>=1.4.0",
    # 14. Spell Checker
    "codespell>=2.2.0",
    # 15. Docstring Coverage
    "docstr-coverage>=2.3.0",
    # Orchestration
    "tox>=4.12.0",
]
# Mutation testing requires coverage<6.0 — conflicts with coverage>=7.4.0
# Install separately: pip install -e ".[mutation]"
mutation = [
    "mutatest>=3.0.0",
]

# --- Tool Configurations ---

[tool.black]
# Deprecated in favor of Ruff, but kept for compatibility if needed.
line-length = 100
target-version = ["py312"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "B",   # flake8-bugbear
    "S",   # flake8-bandit (Security)
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "PL",  # pylint
    "RUF", # ruff specific
    "C90", # mccabe complexity
]
ignore = []

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "PLR2004", "PLC0415", "E501"] # Allow asserts, magic numbers, lazy imports, and long lines in tests
"scripts/*" = ["E501", "S603", "S602", "S105", "S106", "S311", "PLC0415", "PLW1508", "PLR2004", "PLR0915", "PLR1722", "B007", "E402", "UP022"] # Relaxed for utility scripts

[tool.mypy]
python_version = "3.12"
strict = true
ignore_missing_imports = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.coverage.run]
branch = true
source_pkgs = ["claude_memory", "dashboard"]
omit = ["tests/*", "scripts/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    'class .*\bProtocol\):',
    '@(abc\.)?abstractmethod',
]
fail_under = 70

[[tool.mypy.overrides]]
module = [
    "falkordb.*",
    "pyvis.*",
    "streamlit.*",
    "sentence_transformers.*",
    "graphiti_core.*"
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "tests.*"
allow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-v"
filterwarnings = [
    "ignore::coverage.exceptions.CoverageWarning",
    "ignore::requests.exceptions.RequestsDependencyWarning",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "invariant: marks tests as system invariant checks",
]



[tool.codespell]
skip = "*.lock,*.json,*.html"
ignore-words-list = "ne,falkordb,nd"

[tool.docstr_coverage]
skip_magic = true
skip_file_docstring = false
skip_init = true
skip_class_def = false
skip_private = true
fail_under = 80
